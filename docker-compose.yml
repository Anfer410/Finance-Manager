services:
  db:
    container_name: finance_manager_db
    profiles: [db]
    image: postgres:18
    environment:
      POSTGRES_DB: ${DB_NAME:-finance-manager}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      TZ: ${TZ:-UTC}
    ports:
      - "127.0.0.1:${DB_PORT:-5432}:5432"
    volumes:
      - ${DB_DATA_PATH:-./data/db}:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-finance-manager}"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    container_name: finance_manager_app
    image: ghcr.io/anfer410/finance-manager:${IMAGE_TAG:-latest}
    env_file: .env
    environment:
      APP_ENV: prod
    ports:
      - "${APP_PORT:-8080}:8080"
    depends_on:
      db:
        condition: service_healthy
        required: false
    restart: unless-stopped
